Algoritmo "correcao de soma"

Var

numero, soma : real
i : inteiro

Inicio

// entrada de 4 numeros
// processamento somar os 4 numeros
// resultado da soma

para i de 1 ate 4 faca
     escreval("Digite o", i , "° número")
     leia (numero)
     
     soma <- soma + numero
fimpara

escreval ("A soma dos numeros é de : ",soma)

Fimalgoritmo